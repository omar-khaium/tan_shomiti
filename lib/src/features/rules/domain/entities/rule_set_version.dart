class RuleSetVersion {
  const RuleSetVersion({
    required this.id,
    required this.createdAt,
    required this.json,
  });

  final String id;
  final DateTime createdAt;
  final String json;
}

